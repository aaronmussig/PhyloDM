language: python
os:
  - linux
#  - osx
#  - windows
python:
  - "3.6"
#  - "3.7"
#  - "3.8"
before_install:
  - python --version
  - pip install -U pip
  - pip install coverage codecov cython
install:
#  - pip install -vv --no-build-isolation  .
  - pip install -vv dist/phylodm-*.tar.gz
script:
  - coverage run -m unittest discover -s tests/
after_success:
  - codecov
